'use strict';
angular.module('ngRobbersLanguageGenerator', []).constant('robbersLanguageGeneratorConfig', {
  vow: 'o'
}).filter('robbersLanguageGeneratorFilter', [
  'robbersLanguageGeneratorConfig', function(config) {
    return function(textToTranslate, vow) {
      var char, consonants, i, len, ret, selectedVow;
      if (vow == null) {
        vow = void 0;
      }
      selectedVow = vow || config.vow;
      consonants = ['b', 'c', 'd', 'f', 'g', 'h', 'j', 'k', 'l', 'm', 'n', 'p', 'q', 'r', 's', 't', 'v', 'w', 'x', 'y', 'z', 'å'];
      ret = '';
      for (i = 0, len = textToTranslate.length; i < len; i++) {
        char = textToTranslate[i];
        if (consonants.indexOf(char.toLowerCase()) !== -1) {
          ret += [char, selectedVow, char.toLowerCase()].join('');
        } else {
          ret += char;
        }
      }
      return ret;
    };
  }
]);
